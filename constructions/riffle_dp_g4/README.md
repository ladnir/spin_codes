# Riffle DP g=4

**Status:** `FROZEN_REFERENCE`

This folder identifies the frozen one-lap Riffle DP g=4 construction. The
construction remains a reference and is not the active proof target.

The construction uses four-bit packet permutation and the deterministic DP
convolution. It does not include the retained-state second lap or independent
packet multipliers.

Historical artifacts remain in `explorations/` and `scripts/` with the
`riffle_dp_g4` prefix. The continuation record summarizes the frozen map and
the proof checkpoints:

- `../../CONTINUE_RIFFLE_PACKET8_PROMPT.md`

No result for Riffle DP-2Lap g=4 or Riffle PacketMul-2Lap g=4 transfers to
this construction without an explicit reduction.

