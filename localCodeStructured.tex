% Compatibility wrapper for the original combined structured local-code source.
% The compiled paper now inputs the split files directly:
% localCodeOuter.tex, localCodeInner.tex, localCodeCertificate.tex, and
% localCodeProjections.tex.  Do not input this wrapper together with those
% files, since the theorem labels would be duplicated.
\input{localCodeOuter}
\input{localCodeInner}
\input{localCodeCertificate}
\input{localCodeProjections}
