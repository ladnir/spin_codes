cmake_minimum_required(VERSION 3.20)
project(RedesignDiagnostics LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
get_filename_component(ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../../.." ABSOLUTE)
set(QUARTER "${ROOT}/workstreams/rate_quarter_bch/implementation/generated")
set(VENDOR "${ROOT}/constructions/riffle_parityfanout31x33_bchperm_transpose_bitshuffle_splitstate_preaddmul_rm2sub_t128_s19/implementation/vendor")
add_executable(accumulator_bench accumulator_benchmark.cpp "${QUARTER}/QuarterCircuit.cpp")
target_include_directories(accumulator_bench PRIVATE "${ROOT}/workstreams/bare_bch_rm2sub" "${QUARTER}" "${VENDOR}")
target_compile_options(accumulator_bench PRIVATE -march=znver4 -mavx2 -mpclmul -mvpclmulqdq)
