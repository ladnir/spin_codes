\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{pgfplots}
\usetikzlibrary{patterns}
\usepackage{placeins}
\pgfplotsset{compat=1.18}
\usepackage[hidelinks]{hyperref}

% A paper-level contents list for drafts, rather than LLNCS's volume-level list.
\ifsubmission\else
  \setcounter{tocdepth}{2}
  \makeatletter
  \renewcommand{\tableofcontents}{%
    \section*{\contentsname}%
    \begingroup
      \let\l@title\@gobbletwo
      \let\l@author\@gobbletwo
      \let\authcount\@gobble
      \@starttoc{toc}%
    \endgroup}
  \makeatother
\fi

% Use LLNCS's theorem and proof environments and their native typography.
\let\lncsendproof\endproof
\renewcommand{\endproof}{\qed\lncsendproof}

% Keep artifact descriptions, but suppress identifying links during review.
\newcommand{\artifactlink}[2]{\ifsubmission#2\else\href{#1}{#2}\fi}

\newcommand{\F}{\mathbb F}
\newcommand{\E}{\mathbb E}
\newcommand{\Prob}{\mathbb P}
\newcommand{\wt}{\operatorname{wt}}
\newcommand{\supp}{\operatorname{supp}}
\newcommand{\Acc}{\operatorname{Acc}}
\newcommand{\1}{\mathbf 1}
\newcommand{\papertodo}[1]{%
  \par\smallskip\noindent
  \textcolor{red!70!black}{\textbf{TODO.} #1}%
  \par\smallskip}

\allowdisplaybreaks
