1. read and comment what doesnt make sense
2. finish expander section
    a. do expand accumuale (SRINI)
    b. do expand-convolute (SRINI)
3. read section 6.1 and verify
4. Evaluation - the code that computes the MD distributions for permute, convolute, expand
    a. Implement the fix (see 5a)
5. convolute-permute-convolute
    a. Fix: consider inputs with just a bunch of 0s at the end (STAN - 6.1)
6. impossibility result (PETER)
    a. acc-perm-acc can not have linear MD. 
    b. conv-perm-conv might?
7. initial sections


bonus:
    a. can we speedup the MD computation. sparse?
    b. sparse conv (large sparse windows)